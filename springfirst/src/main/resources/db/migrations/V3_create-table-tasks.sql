CREATE TABLE task (
                      id UUID,
                      name VARCHAR(255),
                      description TEXT,
                      username VARCHAR(255),
                      done BOOLEAN,
                      created_at TIMESTAMP
);